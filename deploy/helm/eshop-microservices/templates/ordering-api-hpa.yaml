{{- if .Values.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "eshop.fullname" . }}-ordering-api
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "eshop.labels" . | nindent 4 }}
    app.kubernetes.io/component: ordering-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "eshop.fullname" . }}-ordering-api
  minReplicas: {{ .Values.autoscaling.orderingApi.minReplicas }}
  maxReplicas: {{ .Values.autoscaling.orderingApi.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.orderingApi.targetCPUUtilizationPercentage }}
  {{- if .Values.autoscaling.orderingApi.targetMemoryUtilizationPercentage }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.autoscaling.orderingApi.targetMemoryUtilizationPercentage }}
  {{- end }}
{{- end }}
